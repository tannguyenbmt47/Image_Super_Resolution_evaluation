torch>=2.0
torchvision>=0.15
numpy
pyyaml
tqdm
pillow
# optional, used by the LPIPS metric (perceptual quality)
lpips
# no-reference metrics: NIQE / MUSIQ / CLIPIQA
pyiqa
numba>=0.59
# SwinIR official architecture (timm helpers)
timm
# tests
pytest
